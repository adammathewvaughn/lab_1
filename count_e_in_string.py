def count_e_in_string(str):
    """
    This function iterates through a string, counts the number of "E"s in it, and returns the number as an integer.
    """
    string = "Hello Everyone!"
    return int(string.count("E"))


print(count_e_in_string(int))